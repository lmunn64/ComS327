#include <stdio.h>

enum{ //create a set of named constants
    read_text, // = 0
    write_text, // = 1
    read_binary, // = 2
    write_binary, // = 3
    invalid_action,  // = 4
};

int main(int argc, char *argv[]){
    
    
    return 0;
}